export const arrayReplaceElement = (newElement = [], array = [], index) => {
  let newArray = array;
  if (index !== 0 ? !!index : true, !!newElement) {
    array[index] = newElement;
    newArray = array;
    return newArray;
  }
  return newArray;
}
